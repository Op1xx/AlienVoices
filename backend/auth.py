# Логика авторизации вынесена в методы модели User (backend/db.py):
# user.set_password(raw) — хеширует пароль MD5+salt
# user.check_password(raw) — проверяет пароль
# Этот модуль зарезервирован для будущих хелперов авторизации.
